services:
  app:
    build:
      context: .
    working_dir: /workspace
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
    ports:
      - "5173:5173"

volumes:
  node_modules:
